from .prompts import translate_word, dictionarize_word, generate_sentence
# Legacy compatibility exports (some notebooks may still import these)
# The current runtime uses generate_audio_binary directly.
