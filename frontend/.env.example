# Copy this file to .env and customize as needed.
# Comma-separated list of hostnames that are allowed to reach the Vite dev server.
# Useful when serving the frontend through a Cloudflare tunnel or remote IP.
VITE_ALLOWED_HOSTS=your-domain.example.com

# Optional override if your API is available behind a different hostname/port.
# VITE_API_URL=https://your-domain.example.com/api
